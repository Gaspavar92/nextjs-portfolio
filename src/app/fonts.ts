import { Inter } from "next/font/google";
import { Bangers } from "next/font/google";
import { Protest_Riot } from "next/font/google";
import { Titan_One } from "next/font/google";
import { Gloria_Hallelujah } from "next/font/google";
import { Yatra_One } from "next/font/google";

export const inter = Inter({ subsets: ["latin"] });
export const bangers = Bangers({weight: ["400"], subsets: ["latin"]})
export const protest = Protest_Riot({weight: ["400"], subsets: ['latin']})
export const titan = Titan_One({weight: "400", subsets: ["latin"]});
export const gloria = Gloria_Hallelujah({weight: "400", subsets: ["latin"]});
export const yatra = Yatra_One({weight: "400", subsets: ["latin"]});